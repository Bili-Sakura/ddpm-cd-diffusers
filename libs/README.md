# libs/

This directory is reserved for third-party code or utility libraries that
are not part of the main `src` package.

Examples of what might live here:
- Vendored / modified external libraries
- Dataset preparation scripts not tied to the training pipeline
- Evaluation tools from other repositories
